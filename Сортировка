Или можно не доходит до самого конца?
Принимаются разные вариации и небольшая пояснительная записка "почему вы сделали.
именно так, какой вопрос ускорился"
Следующий вопрос, который необходимо разрешить:
Научить алгоритм понимать, что если перестановок сделано не было,
значит, список уже отсортирован и можно заканчивать алгоритм.

a = [2, 34, 67, 1, 69, 9, 3, 44, 11, 78]

 

n = len(a)

sorted_flag = False  # Флаг, который указывает, отсортирован ли уже список

 

for i in range(n):

    # Изменен диапазон цикла: не доходим до самого конца, так как последние элементы уже отсортированы

    for y in range(n - 1 - i):

        if a[y] > a[y + 1]:

            a[y], a[y + 1] = a[y + 1], a[y]

            sorted_flag = False  # Если была хоть одна перестановка, то список еще не отсортирован

 

    # Добавлен блок, проверяющий флаг отсортированности

    if sorted_flag:

        print('Сортировка уже была')

        break

    else:

        sorted_flag = True  # Сбрасываем флаг перед следующей итерацией

 

print(*a)
